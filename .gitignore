# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# venv
.venv/
venv/
env/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Build / dist
build/
dist/
sdist/

# Logs / scratch
*.log
logs/
nul

# Generated artifacts (these belong in `reports/` instead)
.v9/
.checkpoints/
__pycache__/

# Harness-specific: never commit solver outputs
# NOTE: patterns must use `*.sim` to match `name.sim` — the old `.sim` rule
# only matched a literal file/dir named exactly ".sim" and let 5.5GB of
# `rotor37_*.sim` slip through (caught before pushing 2026-06-17).
*.sim
*.sim.tmp
.sim/
starccm_sessions/
starccm_cli_sessions/
# Rotor37 reproduction: full solver run dir is 5.5GB of regenerable STAR-CCM+
# state — commit only the small run manifest JSONs individually.
reproductions/**/runs/*.sim

# Local smoke-test artifacts (generated by `python -m cfd_harness.cli.run`)
reports_test/

# Mock fixtures that are large / regenerate
fixtures/runs/*/audit_real_run_*.yaml
fixtures/runs/*/real_incident_*.yaml
!fixtures/runs/lid_driven_cavity/audit_real_run_measurement.yaml
!fixtures/runs/lid_driven_cavity/real_incident_measurement.yaml
!fixtures/runs/naca0012_airfoil/audit_real_run_measurement.yaml
!fixtures/runs/naca0012_airfoil/real_incident_measurement.yaml
!fixtures/runs/circular_cylinder_wake/audit_real_run_measurement.yaml
!fixtures/runs/circular_cylinder_wake/real_incident_measurement.yaml

# Pythonista / macOS leftovers (in case of cross-platform work)
.DS_Store

# Local overrides
.env
.env.local
*.local

# Local LDC runner logs (ephemeral, regenerated on each run)
ldc_full_run.err
ldc_full_run.log
ldc_full_runner.err
ldc_full_runner.out
ldc_smoke_runner.err
ldc_smoke_runner.out
ldc_5000_runner.err
ldc_5000_runner.out
all_anchor_smoke.log
all_anchor_final.log
all_anchor_final2.log
main_tests.log
main_tests_q.log
naca_smoke.log
naca_smoke2.log
ldc_smoke*.log
probe_*.log

# Runtime CLI outputs (regenerated each run)
cli_runner.out
cli_runner.err
reports/<case>/<timestamp>/data.json
reports/<case>/<timestamp>/data.md

# ── Takeover stabilization additions (2026-06-13) ──────────────────────
# Agent tooling / scratch that must never enter this repo.
# NOTE: gitignore only treats '#' as a comment at the START of a line —
# inline comments after a pattern become part of the pattern, so keep
# every comment on its own line.
# OpenCode harness node_modules + lockfile (~115 MB)
.opencode/
# Mavis scratch plans
.mavis/
# javac probe scratch
.javac_check/
# compiled probe build leftovers
probe-build.bak/
# prior recon run output
reports_takeover_recon/

# Compiled Java macro artifacts (commit the *.java source, not the *.class)
*.class
macros/**/*.class

# Runtime error logs not matched by *.log (e.g. starccm_gui_start.log.err)
*.log.err

# Regenerable solver / geometry binaries (commit the generator .py, not these)
Cases/
stl_samples/
scripts/*.stl
scripts/*.step
scripts/*.stp
scripts/*.png

# Rotor37 reproduction: large geometry tensors regenerable from deeplabs
# source data (see reproductions/*/assets/SOURCES.md). 2 × 62MB.
reproductions/**/assets/*.npy

# Timestamped V&V run outputs (the literal-path rules above never matched)
reports/*/[0-9]*T*Z/
reports/audit/*/[0-9]*T*Z/
reports/_*.txt

# ── 2026-06-13 takeover stabilization additions ──────────────────────
# Local workbuddy memory (per-project, regenerated each session).
# NEVER commit — these contain per-session state, not repo-wide facts.
.workbuddy/

# DEC-009 hollow-green exclusion: Rotor37Slice2D.java prints DONE and
# exits 0 while solving nothing (uses the dead
# AutoMeshDefaultValuesManager.setValue(double) path on 2402 R8, and
# the geometry is still an LDC placeholder). Committing it would
# mislead future readers. The 8 月期 real implementation lands in
# Rotor37SingleChannel.java (per DEC-008 §4 P0).
macros/Rotor37Slice2D.java

# Trained surrogate model binaries (regenerable from the M3 pipeline)
/models/
# V&V smoke-run outputs (regenerable; like reports/<case>/<ts>/)
reports/_smoke/
